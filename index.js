// // const divide = (dividend, divisor) => dividend / divisor; 
// const divide = (2000, 100) => 2000 / 100; 
// divide
// // divide(2000, 100);

function divide (dividend = 2000, divisor = 100){
    return dividend/divisor;
}
divide(2000,100);

const square = x => x*x;
const add = (num1, num2) => num1+num2;
